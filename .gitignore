# Built mod files — regenerated by build.py, no need to track
*.ts4script
*.package
# Release archives -- built by `python build.py --release`, named
# Llamafone_v3.6.1.zip etc. Never track built artifacts in git.
Llamafone_*.zip
Llamafone.zip

# Local Claude Code config -- personal, don't share.
.claude/

# Python cache
__pycache__/
*.pyc
*.pyo

# Embedded Python 3.7 interpreter (download-it-yourself, see README)
tools/python37/
python37.zip
mc_*.txt
analyze_pyc.py
decompile_test.py
*.txt

# But the site needs robots.txt to be tracked
!docs/robots.txt

# Local decompile + parser scripts, game-package extracts
.research/
_dump_*.py
